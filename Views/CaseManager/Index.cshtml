@* @model List<CaseManager> *@
@using CrimeManagement.Data
@model List<ApplicationUser>



<h1 class="row justify-content-center">Case Managers</h1>

    <table id="managerData" class="table" style="width:100%">
        <thead>
            <tr>
                @* <th>ID</th> *@
                <th>First Name</th>
                <th>Last Name</th>
                <th>Cases</th>
            </tr>
        </thead>
        @* <tbody>
            @foreach (var caseManager in Model)
            {
                <tr>
                    <td>@caseManager.Id</td>
                    <td>@caseManager.FirstName</td>
                    <td>@caseManager.LastName</td>
                    <td><a asp-controller="CaseManager" asp-action="ViewCase" asp-route-caseManagerId="@caseManager.Id">View</a></td>
                </tr>
            }
        </tbody> *@
    </table>


@section Scripts {
    <script src="~/js/Managers.js"></script>
}